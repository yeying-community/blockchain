# 知行图 · ZhixingGraph

> **算力即电力，认知即资产，人类共识即地图。**

一个以认知为锚定物的图谱协议（cognition-anchored graph protocol）：把**电力 → 算力 → 认知**逐层映射并证明，代币 $COG 仅在产生**可验证的认知增量（ΔK）** 时增发——从根本上区别于"为挖矿而挖矿"的 PoW 与无价值锚定的空气币。

由**夜莺社区（Nightingale Community）** 发起，当前处于构想草稿（RFC）阶段。

## 文档

- 📄 **[白皮书 · WHITEPAPER.md](docs/WHITEPAPER.md)** — 哲学基础、三层价值映射、PoK 共识、代币经济学、技术架构与路线图。
  - 附录 B 深化：[B.1 博弈论与攻击面](docs/WHITEPAPER.md#b1-pok-共识的博弈论建模与攻击面分析)、[B.2 ΔK 形式化与规范](docs/WHITEPAPER.md#b2-δk-计算公式的形式化定义与参数标定)、[B.3 经济仿真](docs/WHITEPAPER.md#b3-cog-增发销毁的经济仿真agent-based-simulation)、B.4 绿证对接、B.5 zkML/opML、B.6 本体标准。

## 仿真

- 🧪 **[`sim/`](sim/)** — 零依赖（纯 Python 标准库）的经济学 agent-based 仿真，验证 PoK 激励与攻击抵抗性。

```bash
python3 sim/run.py          # 运行场景矩阵
python3 sim/run.py --md     # 并写出 sim/RESULTS.md
```

核心结论（详见 [sim/README.md](sim/README.md)）：在女巫 + 合谋攻击场景下，**攻击者净回报为负（亏损），诚实贡献者为正**，伪贡献通过率随声誉机制收敛到 0。

## 性能引擎（Rust）

- ⚙️ **[`engine/`](engine/)** — 认知图谱热路径（kNN + ΔK）的 Rust 实现（纯 std、零依赖），与 Python 参考实现共享同一份 B.2.3 契约。

```bash
cd engine && cargo run --release --bin bench
```

同工况下 **Rust ≈ 230× 于 Python**（10,900 vs 47 submissions/sec），校验和一致。引擎还通过 **pyo3** 绑定导出为 Python 模块，让仿真直接调用 Rust 热路径——整条 ABM 仿真 **≈ 96×** 提速且结果按种子逐字节一致：

```bash
./engine/build_python.sh      # 构建 pyo3 扩展模块（cargo + abi3，无需 maturin）
python3 sim/run.py --compare  # baseline 场景 Python vs Rust 后端对比
```

详见 [engine/README.md](engine/README.md)。

## 参考节点（Rust · PoK 共识状态机）

- ⛓️ **[`node/`](node/)** — 把 PoK 规则落成一个**确定性的共识状态机**：区块 / 交易 / 账户 / 状态转移 / 铸造罚没 / 链上声誉 / ed25519 签名交易 / 追加式持久化 / 确定性 mempool 出块 / Merkle 认证状态与轻客户端证明 / BFT 最终性证书 / BFT 轮次状态机（超时·锁定·换轮）/ 逐高度生长的 BFT 认证链 / 证书落盘 + 重放即最终性复验 / 链上·动态验证人集（跨高度增删改权，重放跟随）/ P2P gossip 与反熵状态同步（交易泛洪 + 认证块追赶，含真实 TCP）/ 质押绑定的验证人权重与解绑期（自绑定 $COG → 权重、解绑经时间锁提款）/ 按证据罚没等价双签（链上双签证据 → 罚没绑定质押入 treasury、移出验证人集）/ P2P 传播块级 ops（双签证据 + 质押变更的 flood + 节点待打包池，下一区块由出块方带出，作恶可远程归责）/ 验证人集变更的轻客户端跟随协议（只凭创世 + (块,证书) 对逐高度跟随活跃验证人集，不执行交易、与全量重放逐字节一致）/ 验证人集 Merkle 承诺入区块头（`next_validators_root` 折进 `block_hash`，轻客户端免复刻迁移验证整套下一集合、或用 O(log n) 包含证明对 cert 签名头证明单个验证人，SPV 原语）/ 头部的轻同步传输（`BlockHeader` 携每份体的 SHA-256 承诺 + `CertifiedHeader` + `GetHeaders/Headers` + `LightGossipNode` 从不解码交易）/ 钱包的账户-成员 SPV（`BlockHeader` 再携 `state_root` 完整共识状态 digest + `accounts_root` accounts/reviewers 二叉 Merkle 根两条承诺根；新 `GetAccountProof/AccountProof` 让钱包从对端要单账户包含证明；新 SPV `verify_account_membership_against_header` 在本地重算 leaf 并对头里的 `accounts_root` 验证——钱包证明自己的余额**只下头、不下体、零重放**）/ **泛化的批量化证明请求总线**（M24：`GossipMsg::GetProof { items }` / `Proof { items }` 一对承载 **任意** `(kind, id)` 列表——`kind ∈ { Account, Reviewer, Validator }`，上限 32；新 `Reviewer::merkle_leaf()` + `ChainState::reviewer_proof(id)` 闭合审阅人路径；新 `ProofEntry` 枚举承载 typed leaf + proof；wallet 端**唯一**的 SPV 验证器 `ValidatorTracker::verify_proof_against_header(header, cert, tracked_set, entry)` 按 entry 的 `ProofKind` 选根——Account/Reviewer 对 `accounts_root`，Validator 对 `next_validators_root`，本地重算 leaf、零信任 prover）// **图节点的 cert-signed 包含证明**（M25：`engine::GraphNode` 加单调 `node_id` 字段，`ChainState::merkle_leaves` 增第三段承载 graph 节点（插入序），`BlockHeader.accounts_root` 自动扩展覆盖 accounts ∪ reviewers ∪ graph 三集合的同一 Merkle 根；新 `ProofKind::GraphNode = 3` 与 `ProofEntry::GraphNode { node_id, graph_node, proof }` 走 M24 同一 `GetProof`/`Proof` 总线；`verify_proof_against_header` 加 `GraphNode → accounts_root` 一支，wallet 端仍是**唯一**的验证器+零信任 prover；`ChainState::graph_node_proof(idx)` 闭合图节点的 O(log n) 证明路径，认知图谱不再仅是 `state_root` 的扁平 digest）// **图节点的 cert-signed 邻域证明**（M26：`engine::CognitiveGraph::k_nearest_with_ties(query, k)` 按 cosine 降序+`node_id` 升序稳定排序，边界 ties 全留（`len ≥ k`）；新 `KnnClaim { query, k, neighbours: Vec<(node_id, GraphNode, merkle::Proof)> }` + `ValidatorTracker::verify_knn_against_header` 把**单一** cert-signed header 拆成 (1) header/cert 绑定、(2) 每个 neighbour leaf 对 `accounts_root` 的 Merkle 验证、(3) 本地用 `cos_sim` 重排+同 cut、(4) 与 prover 序列等比——prover 不可省略 tied 邻居也不可重排；钱包**仍**只需这一入口即可“证明图谱在某个 embedding 周围的邻域”，认知图谱本身成为可证明查询对象）/ **图节点的 cert-signed 范围查询**（M27：`BlockHeader` 新增 32-byte `graph_root` 槽位、对同一 cert-signed header 提交——`graph_merkle_root()` 按 `(cos_sim(CANONICAL_PIVOT, n.embedding) desc, node_id asc)` 排序索引（`CANONICAL_PIVOT = [1,0,0,0,0,0,0,0]`，确定且 query-无关）——区别于 M25 的 `accounts_root` 插入序；新 `ChainState::graph_range_proof(a, b)` 返回 `(sub_root, Vec<(node_id, GraphNode, Proof)>)`；新 `RangeClaim { query, min_sim, nodes }` + `GossipNode::serve_range(query, min_sim)` 把 cosine-cutoff `(query, min_sim)` 派给全节点；wallet 端 `ValidatorTracker::verify_range_against_header` 把单一 cert-signed header 拆成 (1) header/cert 绑定、(2) `min_sim ∈ [-1, 1]` cutoff 验证、(3) 每个 node leaf 对 `graph_root`（**非** `accounts_root`）的 Merkle 验证、(4) 本地用 `cos_sim` 重排+`sim >= min_sim` 的 prefix cut、(5) 与 prover 序列等比——prover 不可重排、不可漏（重排）/ **图节点的 cert-signed 时序 diff**（M28：在 M25 单点 + M26 邻域 + M27 同高度范围之外，闭合**时序轴**——钱包问"h₁ 到 h₂ 之间图节点 added/dropped 是哪些"无需下载两套图。新 `GraphLeafAtHeight { node_id, graph_node, proof }` + `DiffClaim { added, dropped }`（`changed` 臂在 M25 append-only 下**结构上不可达**，删除）；新增 `ChainState::graph_diff(&prev_state)` 在 producer 侧构造 `{added, dropped}`，每个 leaf 的 `proof` 对**各自高度**的 `accounts_root`；新增 `DiffEnvelope { header_prev, cert_prev, header_new, cert_new, diff, tracked_set_h1, tracked_set_h2 }` + `GossipNode::serve_diff(h1, h2, …)`（缓存 genesis 在 GossipNode 上可重放 h₁ 状态）；wallet 端 `ValidatorTracker::verify_diff_against_headers` 拆为 (1) 两边 header/cert 绑定 + 两边 tracked set 各自验签（动态验证人集下两个高度用不同集合）、(2) **wallet 端局部重放** `[1..=h₂]` 取 `state_at_h2` + `[1..=h₁]` 取 `state_at_h1`（与 prover 用的同一重放路径），(3) 每个 `added` leaf 对 `header_h2.accounts_root`、每个 `dropped` leaf 对 `header_h1.accounts_root` 的 Merkle 验证，(4) prover 与重放派生的 `added`/`dropped` 集合等比——**完整性由重放保障，单 leaf 证明只验身体**；**不用 `graph_root`**，因 cosine 排序不保 `node_id` 序，无法界定 diff 大小——M28 一律走 accounts_root（插入序）；wire tags `TAG_GETDIFF=10` / `TAG_DIFF=11`）/ **信任无关跨链桥**（M30：同协议两条链 A↔B（不同创世 → 不同 `genesis_hash`、对称可互为源/目的）走**锁 + 验证 + 重放去重**三步——`verify_lock` **无新 SPV 逻辑**，仅复用 M22 cert-binding + M25–M28 Merkle inclusion。新 `BridgeLock { account, amount, dest_chain: Hash, dest_account, nonce, signature }`（签名 op 镜像 `StakeOp`，验签 + `amount != 0` + 余额覆盖 + account 已知，错误复用 `ZeroStake`/`BadSignature`/`UnknownAccount`/`InsufficientBalance`）；`Block.bridge_root` 累计 bridge_locks Merkle 根入 cert-signed prefix 槽（与 `graph_root` 同位、`Chain::seal` 盖/`apply` 强制度 + 新 `ChainError::BridgeRootMismatch`），头尾部 `bridge_locks_commitment` 与 `txs_commitment` 同列；`Block.bridge_locks: Vec<BridgeLock>` 与 stake_ops/slashing_evidence 同列进 apply；`ChainState` 增 `bridge_locked: u64`（新供应组分、supply 不变量扩展为 `Σ余额 + treasury + bonded + unbonding + bridge_locked == supply`，lock 是 supply 内部再分配）/ `bridge_locks: BTreeMap<u64, BridgeLock>` / `bridge_lock_heights: BTreeMap<u64, u64>` / `next_lock_id: u64`；`state_root` digest 多 fold `bridge_locked` / `bridge_locks` 各 `lock.merkle_leaf(id)` / `bridge_lock_heights` / `next_lock_id`。新模块 `node/src/bridge.rs`：`LockEnvelope { source_header, source_cert, source_tracked_set, lock_id, lock, proof }`（M28 `DiffEnvelope` 同形）+ `BridgeEndpoint { my_genesis_hash, source_genesis_hash, tracker: ValidatorTracker, consumed: BTreeSet<(Hash,u64)>, minted: BTreeMap<u64,u64> }` + `VerifiedLock` + `BridgeError::{Cert(LightError), WrongDestination, AlreadyConsumed, SourceNotFollowed}`；`verify_lock(env)` = (1) cert-binding 复用 M22 `verify_state_root_against_header(self.tracker.validators(), cert)`（**endpoint 自己的集合，不用 envelope 的**——relayer 无法替换验证人集）+ (2) inclusion `merkle::verify(&env.source_header.bridge_root, &leaf_hash(&lock.merkle_leaf(lock_id)), &env.proof)` + (3) dest match `lock.dest_chain == my_genesis_hash` + (4) replay `(source_genesis, lock_id) ∉ consumed`；`consume(v)` 入 dedup + `minted[dest_account] += amount`（bridge-module 记账，consensus 不感知 mint）。新 wire 对 **TAG_GETLOCK=14 / TAG_LOCK=15** + `GossipNode::serve_lock(lock_id)`（用 `bridge_lock_heights` 取出块高 → 从 retained chain 取 header+cert，重放 `[..=height]` 取 active set 作 `source_tracked_set`）；`LightGossipNode::locks: Option<LockEnvelope>` + `take_lock()`。**信任无关**：relayer 只搬运字节、无法伪造 A 验证人未签的锁——`node bridge` CLI 演示正路径 + tampered-proof / wrong-destination / replay / tampered-root 四类 `BridgeError` 负测）/ **共识级跨链赎回 + 铸造**（M31：把 M30 off-chain 的 mint+dedup 搬进**目的链状态机**——两个自认证 op `BridgeHeader`（推进链上源链跟随器 `BridgeSource`）+ `BridgeRedeem`（对源 cert-signed `bridge_root` 验锁后铸造到 `dest_account`）；`Genesis.bridge_sources` 声明允许源（源创世 hash + 验证人集 = 信任锚），`ChainState` 加 `genesis_hash`（排除出 `state_root` 因循环）/ `bridge_minted` / `bridge_sources`，redeem 同增 `balance` 与 `supply` 故供应守恒不变；relayer 仍信任无关，唯 A 验证人签名 + cert-signed `bridge_root` 授权 mint——`node redeem` CLI 演示正路径 + tampered-proof/`BridgeInclusionInvalid`、tampered-root/`BridgeCertInvalid`、wrong-dest/`BridgeWrongDestination`、replay/`BridgeAlreadyRedeemed`、not-followed/`BridgeSourceNotFollowed` 五类负测）/ **联网 tokio 守护进程（单定序器测试网）**（M32：`node run --config` 起长驻 tokio 守护进程——`src/daemon.rs` 单属主 actor（`GossipNode` 独占一个 task、per-peer mpsc 出站、无 `Arc<Mutex>`），把 M6–M31 的纯状态机（`GossipNode::on_message` / `apply_certified` / `BlockLog` / `CertLog`）从进程内 `VecDeque` 总线搬上真实 TCP：`u32` BE 长度前缀 + `encode_gossip` 分帧且加 `MAX_FRAME = 16 MiB` 上限（阻塞版 `read_msg` 无上限），8 字节 BE id 握手在 `GossipMsg` 之外（wire tag 范围不动），只向 id 更大的 peer 拨号 → 每对恰一条连接；一个 `[producer].enabled` 节点为**过渡期定序器**，持验证人 seeds 经既有进程内 `Sim`/`ChainDriver` 定稿证书块，其余全节点经 socket 同步 + 逐块复验证书（分布式 BFT 投票留 M33）；actor 是本节点日志唯一写者，boot 经 `load_certified` / `ChainDriver::resume` 复验最终性恢复；新 `src/config.rs`（serde + toml 镜像结构、共识类型仍 serde-free）载 `NodeConfig`/`GenesisConfig`/`KeystoreConfig`；`node localnet` 进程内 4 节点经真实 loopback socket 收敛演示 + `testnet/` 样例配置）/ **分布式 BFT 投票（无定序器）**（M33：把 M32 中心化的单定序器换成**真正分布式**的共识——每个验证人节点各持**一把** `Keypair` + 一个 `round::RoundState`，proposal/prevote/precommit 经同一条 TCP 总线 gossip（新 `TAG_CONSENSUS=16` + `GossipMsg::Consensus(Box<round::Msg>)`，proposer 编码的 `Block` 字节与接收端哈希逐字节一致），round 推进由 wall-clock 超时驱动（`PROPOSE/PREVOTE/PRECOMMIT_BASE=1000ms` + 每 round `+500ms` 回退）；`GossipNode::on_message` 仍纯——把 `Consensus` 直接 drop，共识在 actor 主循环里路由到 `RoundState`；config 用 per-node `[validator]{enabled, seed_hex}` 替换 `[producer]`，seed 与 genesis pubkey 不匹配即 fail-fast，`kp=None` 节点是纯 follower；`build_candidate` 永远出一空 sealed block（空块心跳）、`on_consensus` 在 prevote 前用 `Chain::would_accept` 试跑 apply（Byzantine-proposer 保护）、`reconcile_after_sync` 让 anti-entropy 永远优先于尚未决的 round；`Sim`/`ChainDriver` 保留给 `bft`/`live`/`chain` 离线 demo；4 等权验证人 quorum=3 → 3-of-4 持续推进、2-of-4 安全停摆；`localnet` 现为 4 验证人零定序器，测试含 4-验证人收敛 / 1-fault 推进 / 2-fault 停摆 / 晚加入同步 / 纯 follower 跟随）/ 观测到双签即主动罚没（M34：验证人摄入 precommit 时若已持有同 `(validator, height, round)` 的另一 `block_hash` → 组装 `SlashEvidence` 经 `Action::Equivocation` 接入既有 M19 证据 flood → 入块 → 链上没收 bond + 移出 offender；无新 wire/codec/config、纯旁路观测、first-wins 摄入不变）/ 配置驱动共识时序 + `create_empty_blocks`（M35：新增可选 `[consensus]` TOML 段，把轮次超时（propose/prevote/precommit 基础 + 每 round 线性回退增量）与出块节奏 `block_interval_ms` 从编译期常量改为文件可配，`ConsensusConfig::Default` 逐字段等于旧常量、成为唯一真源；加 `create_empty_blocks=false` → 验证人只在有真实待办（mempool ∪ 待打包 stake_ops ∪ 待打包证据，桥不计）时才起轮出块，空闲链暂停不再无限长心跳块；`daemon.rs` 五常量删除、改由 module-private `Timing` 结构承载、`timeout_for` 改纯函数、门只加在被调度的 `on_start_tick`（`on_consensus` lazy-start 不设门 → peer 一提议就跟上，保活性）、`net.rs` 新 `has_pending_work()`；段/字段两级 `#[serde(default)]` 使缺段的老 `testnet/*.toml` 与 `localnet` 逐字节不变）/ 配置驱动网络时序（M36：收尾 M35 顺延的两个**网络/守护进程生命周期**旋钮——新增可选 `[network]` TOML 段把反熵心跳间隔 `announce_interval_ms`（原 `const ANNOUNCE_SECS=2s`）与验证人启动宽限 `startup_delay_ms`（原 `const STARTUP_DELAY=1000ms`）从编译期常量改为文件可配，`NetworkConfig::Default` 逐字段等于旧常量（2000/1000）、成为唯一真源、`daemon.rs` 两常量删除；单位秒→毫秒统一到 M35 的 `_ms` 约定（`2000ms == 2s` 行为一致）；`Node::start` 内联 hoist 出本地变量喂给心跳 `interval` 与启动 `sleep`；段/字段两级 `#[serde(default)]` 使缺 `[network]` 段的老配置与 `localnet` 逐字节不变（仍收敛同一 head）；运维篮子剩余（`tracing` 结构化日志、metrics/health、peer discovery、TLS/auth）顺延至 M37+）/ 内容寻址区块哈希链 + 状态根。ΔK 复用引擎，与白皮书 B.2.3 是同一份契约。引擎纯 std、零依赖、可离线编译；节点作为**应用层**取审计过的 `ed25519-dalek` 做签名，并自 M32 起为联网守护进程引入 `tokio`（异步运行时）+ `serde`/`toml`（配置）——共识核心 `lib.rs` 仍 serde-free。

```bash
cd node && cargo run --release --bin node -- demo    # 内存演示链
cargo run --release --bin node -- build              # mempool 规范排序出块
cargo run --release --bin node -- prove              # 轻客户端 Merkle 证明
cargo run --release --bin node -- bft                # BFT 最终性证书
cargo run --release --bin node -- live               # BFT 活性：轮次状态机驱动出块（含换轮）
cargo run --release --bin node -- chain              # BFT 认证链：mempool → 共识 → 提交，逐高度生长
cargo run --release --bin node -- validators         # 链上验证人集：逐高度增删验证人，重放跟随
cargo run --release --bin node -- gossip             # P2P：反熵同步 + 交易/证据/质押变更 gossip + 真实 TCP
cargo run --release --bin node -- light              # 轻客户端：逐高度跟随验证人集，不执行交易
cargo run --release --bin node -- vprove             # 验证人 Merkle 证明：对 cert 签名头证明单个验证人在下一集合中
cargo run --release --bin node -- lsync              # 头部轻同步：全+光节点同总线，光端只拉头不下体
cargo run --release --bin node -- account            # 钱包 SPV 三证明批量化：账户余额 + 审阅人声誉 + 验证人集合成员，单次 GetProof 往返、本地重算 leaf、对 cert-signed 头验证
cargo run --release --bin node -- graph              # 认知图谱节点 cert-signed 包含证明：单次 GetProof 拿图节点 + 账户，光端对 accounts_root 重算 leaf、零信任 prover
cargo run --release --bin node -- knn                # 图节点 cert-signed 邻域证明：full peer 本地 kNN → KnnClaim，wallet 端 verify_knn_against_header 重排 + cut，对同一 cert-signed 头零信任
cargo run --release --bin node -- range              # 图节点 cert-signed 范围查询：full peer 本地 cosine cutoff → RangeClaim，wallet 端 verify_range_against_header 对 graph_root 重排 + cut，对同一 cert-signed 头零信任
cargo run --release --bin node -- staking            # 质押绑定验证人权重 + 解绑期
cargo run --release --bin node -- slashing           # 按证据罚没等价双签：双签证据 → 罚没入 treasury
cargo run --release --bin node -- certs --dir ./data # 证书落盘 + 重放复验最终性
cargo run --release --bin node -- localnet           # M33：进程内 tokio 测试网（4 验证人，无定序器）经真实 socket BFT 收敛到同一 head
cargo run --release --bin node -- run --config testnet/node21.toml  # M33：联网 tokio 守护进程（TCP P2P gossip + 分布式 BFT 投票，加载 config/genesis/validator）
cargo test --release                                 # 273 项测试（确定性/守恒/回滚/持久化/Merkle/BFT/认证链/最终性复验/动态验证人集/P2P gossip/质押绑定/等价双签罚没/块级 ops 走 gossip/轻客户端跟随验证人集/验证人集 Merkle 承诺入头/头部轻同步传输/钱包 SPV 账户证明/M24 泛化批量化证明请求总线：Account/Reviewer/Validator 同一 GetProof 对、单一 verify_proof_against_header 验证器、reviewer_proof 闭合审阅人路径、MAX_PROOF_BATCH=32 上限 + M25 图节点 cert-signed 包含证明 + M26 cert-signed 邻域证明：GraphNode=3 同 GetProof 对、accounts_root 三集合（accounts ∪ reviewers ∪ graph）同 Merkle 根、graph_node_proof 闭合图路径、engine GraphNode 单调 node_id + M27 cert-signed 图范围查询：header 新增 graph_root 槽位、对同一 cert-signed 头 commit，graph_merkle_root 按 (cos_sim(CANONICAL_PIVOT, n.embedding) desc, node_id asc) 排序索引，graph_range_proof 给定切片 [a,b) 子根+逐叶证明，serve_range 派给全节点 cosine-cutoff (query, min_sim)，wallet 端 verify_range_against_header 对 graph_root 重排 + cut (同一 kNN 同形信任模型) + M28 cert-signed 时序 diff：graph_diff(prev_state) → DiffClaim { added, dropped }，DiffEnvelope 含两边 header/cert/tracked_set，serve_diff 缓存 genesis 字段可重放 h₁ 状态，verify_diff_against_headers wallet 端局部重放 [1..=h₂] + [1..=h₁] 派生 added/dropped，每 leaf 对各自高度 accounts_root 验签+prover 与重放等比 (InvalidDiffRange / DiffMismatch) + M29 异构批 SPV 传输：BatchItem { Inclusion|Knn|Range|Diff } 异构槽位 + BatchResponseEnvelope 一次取齐，serve_batch 复用 M24/M26/M27/M28 四个 serve_* 仅 dispatch (无新 SPV 逻辑)，verify_batch 把每 slot 派回四个现有验证器，MAX_BATCH_ITEMS=32 上限，新 LightError { BatchTooManyItems, BatchItemCountMismatch, BatchItemKindMismatch } 三类仅协议违规错误，wire tags TAG_GETBATCH=12 / TAG_BATCH=13 + M30 信任无关跨链桥：BridgeLock { account, amount, dest_chain, dest_account, nonce, signature } 签名 op，Block.bridge_root 累计 bridge_locks Merkle 根入 cert-signed prefix 槽 + ChainError::BridgeRootMismatch 强制度，Block.bridge_locks: Vec<BridgeLock> 与 stake_ops 同列进 apply，ChainState 多 bridge_locked / bridge_locks(BTreeMap<u64,BridgeLock>) / bridge_lock_heights(BTreeMap<u64,u64>) / next_lock_id 四字段，state_root digest 多 fold bridge 字段，supply 不变量扩展含 bridge_locked (lock 是 supply 内部再分配)；新模块 src/bridge.rs 含 LockEnvelope (M28 DiffEnvelope 同形) + BridgeEndpoint (my/source genesis + ValidatorTracker + consumed dedup + minted ledger) + VerifiedLock + BridgeError { Cert(LightError), WrongDestination, AlreadyConsumed, SourceNotFollowed }；verify_lock(env) 无新 SPV 逻辑 = (1) M22 cert-binding 用 endpoint 自己的 tracker 集合 (relayer 无法替换验证人集) + (2) merkle::verify 对 header.bridge_root 而非 accounts_root + (3) dest match + (4) replay；新 wire TAG_GETLOCK=14/TAG_LOCK=15，serve_lock 用 bridge_lock_heights 取出块高再从 retained chain 取 header/cert/active_set；LightGossipNode 加 locks: Option<LockEnvelope> + take_lock；node bridge CLI 演示正路径 + tampered-proof / wrong-destination / replay / tampered-root 四类 BridgeError 负测 + M31 共识级跨链赎回 + 铸造（目的链链上）：把 M30 off-chain 的 mint+dedup 搬进目的链状态机——两个自认证 op `BridgeHeader`（推进链上源链跟随器 BridgeSource{set,head,height,consumed}）+ `BridgeRedeem`（对源 cert-signed bridge_root 验锁后铸造到 dest_account）；`Genesis.bridge_sources` 声明允许源（源创世 hash + 验证人集 = 信任锚），`ChainState` 加 genesis_hash（排除出 state_root 因循环）/ bridge_minted / bridge_sources(BTreeMap)，Block 加 bridge_headers/bridge_redeems（apply 时 headers 先于 redeems）；apply_bridge_redeem 验证全先于变更（cert-binding + inclusion + dest match + replay dedup + dest 存在）后同增 balance 与 supply 故 supply_conserved 不变、bridge_minted 审计镜像；新 8 类 ChainError（UnknownBridgeSource/BridgeBadFollow/BridgeCertInvalid/BridgeNextSetMismatch/BridgeSourceNotFollowed/BridgeInclusionInvalid/BridgeWrongDestination/BridgeAlreadyRedeemed），codec 头加 bridge_headers/redeems_commitment 两 32B（decode_certified_header +64B），ChainDriver 加 stage_bridge_header/redeem；relayer 仍信任无关（唯 A 验证人签名 + cert-signed bridge_root 授权 mint）——node redeem CLI 演示正路径 + tampered-proof/BridgeInclusionInvalid、tampered-root/BridgeCertInvalid、wrong-dest/BridgeWrongDestination、replay/BridgeAlreadyRedeemed、not-followed/BridgeSourceNotFollowed 五类负测 + M32 联网 tokio 守护进程（单定序器测试网）：把 M6–M31 的纯状态机（GossipNode::on_message / apply_certified、BlockLog/CertLog）从进程内 VecDeque 总线搬上真实 TCP——新 src/daemon.rs 单属主 actor（GossipNode 独占一个 task，per-peer mpsc 出站、无锁），u32 BE 长度前缀 + encode_gossip 分帧且加 MAX_FRAME=16MiB 上限（阻塞版 read_msg 无上限），8 字节 BE id 握手在 GossipMsg 之外（wire tag 范围不动）、只向 id 更大的 peer 拨号故每对恰一条连接；一个 [producer].enabled 节点为过渡期定序器，持验证人 seeds 经既有进程内 Sim/ChainDriver 定稿证书块，其余全节点经 socket 同步 + 逐块复验证书（分布式 BFT 投票留 M33）；actor 是本节点日志的唯一写者（任何命令后 append node.blocks()[appended..]），boot 经 load_certified / ChainDriver::resume 复验最终性恢复；新 src/config.rs（serde + toml 镜像结构，共识类型仍 serde-free）载 NodeConfig/GenesisConfig/KeystoreConfig（自带严格 hex 解码，hash.rs 只编码）；node localnet 进程内 4 节点经真实 loopback socket 收敛演示 + daemon 分帧/超帧/握手/3 节点 TCP 收敛四测 + config 往返/样例载入测 + M33 分布式 BFT 投票（无定序器）：daemon actor 独占 Keypair(Option)+RoundState(Option)+tokio timer，新 TAG_CONSENSUS=16 + GossipMsg::Consensus(Box<round::Msg>) 装 proposal/prevote/precommit（proposer 编码 Block 字节 == 接收端哈希），Cmd::{StartHeight,Timeout} 经 self_tx 自调度、wall-clock 超时（PROPOSE/PREVOTE/PRECOMMIT_BASE=1000ms + 每 round +500ms 回退）；GossipNode::on_message 仍纯（Consensus 直接 drop、actor 主循环路由到 RoundState）；config 用 per-node [validator]{enabled,seed_hex} 替换 [producer]、seed 与 genesis pubkey 不匹配即 fail-fast、kp=None 为纯 follower；build_candidate 永远出空 sealed block（空块心跳）、on_consensus 在 prevote 前 Chain::would_accept 试跑 apply（Byzantine-proposer 保护）+ 早到 proposal 触发 lazily start_height、reconcile_after_sync 让 sync 永远赢；Sim/ChainDriver 保留给 bft/live/chain 离线 demo；4 等权验证人 quorum=3 → 3-of-4 推进、2-of-4 安全停摆；round::decided_block 访问器 + Chain::would_accept + codec consensus_msg 往返 + 四验证人收敛/1-fault 推进/2-fault 停摆/晚加入同步/纯 follower 跟随五测) + M34 观测到双签即主动罚没：round::RoundState::ingest 返回 Option<SlashEvidence>——摄入 precommit 时若已持有同 (validator,height,round) 的另一 block_hash 就按 hash 规范排序组装证据、经新 Action::Equivocation 上抛，Actor on_equivocation 调 submit_local_evidence 接入既有 M19 flood → 入块 → 链上没收 bond + 移出 offender（无新 wire/codec/config，first-wins 摄入不变、纯旁路观测；prevote 双签本模型不罚、纯 follower 不检测但仍转发）：round.rs 五测（precommit 双签出证据 / 同 hash 重发非双签 / 跨轮非双签 / prevote 不可罚 / 证据规范排序跨节点同 hash）+ daemon.rs 一测（TCP 双签注入器 → 证据入块、链上没收 + 移出验证人集）+ M35 配置驱动共识时序 + create_empty_blocks：新增可选 [consensus] TOML 段（propose/prevote/precommit_timeout_ms + timeout_delta_ms + block_interval_ms + create_empty_blocks），ConsensusConfig::Default 逐字段等于旧 daemon 常量（1000/1000/1000/500/1000 + create_empty_blocks=true）成为唯一真源、五常量从 daemon.rs 删除；daemon 改由 module-private Timing copy 结构（Node::start 从 cfg.consensus.* 内联构造）承载、timeout_for 改纯函数 timeout_for(&Timing,step,round)、门只加在被调度的 on_start_tick（空块关且 !has_pending_work → 不起轮按 block_interval_ms 重排；on_consensus lazy-start 不设门 → peer 一提议就跟上保活性）、BLOCK_INTERVAL 全换 self.timing.block_interval_ms；net.rs 新 GossipNode::has_pending_work()（mempool ∪ pending_stake_ops ∪ pending_evidence，桥无待打包池故意不计入）；段/字段两级 #[serde(default)] 使缺段/部分段回落默认（后向兼容）：config.rs 三测（默认等于旧常量 / 缺段回落 / 部分段覆盖）+ daemon.rs 两测（timeout_for 配置化基础+delta / create_empty_blocks=false 空闲暂停→submit 一 tx 后进一非空块）+ M36 配置驱动网络时序：新增可选 [network] TOML 段（announce_interval_ms + startup_delay_ms），NetworkConfig::Default 逐字段等于旧 daemon 常量（2000/1000 = 旧 ANNOUNCE_SECS=2s / STARTUP_DELAY=1000ms，秒→毫秒统一到 _ms 约定）成为唯一真源、两常量从 daemon.rs 删除；Node::start 内联 hoist 出本地变量喂给反熵心跳 interval 与验证人启动 sleep；段/字段两级 #[serde(default)] 使缺段/部分段回落默认（后向兼容、localnet 逐字节不变收敛同一 head）：config.rs 三测（默认等于旧常量 / 缺段回落 / 部分段覆盖）；运维篮子剩余（tracing/metrics/health/peer discovery/TLS/auth）顺延至 M37+）
```

> 共识的前提是确定性：相同创世 + 相同区块 → 逐字节相同的 `state_root`；状态落盘为追加式区块日志，重启重放可完整重建；> 2/3 投票权的最终性证书 + Merkle 状态根让轻客户端可离线验证区块与账户；Tendermint 式轮次状态机在提议人宕机时仍能换轮出块（活性）；驱动器逐高度串起 mempool→共识→提交，长出一条每块附可验证证书的认证链——低于 1/3 宕机仍生长，达 1/3 则安全停摆；证书随区块一并落盘，重放时逐高度复验 > 2/3 证书，恢复的是**最终性**而非仅状态（丢/换/伪造证书都被拒）；验证人集本身是折入 `state_root` 的链上状态，可由区块携带的变更跨高度增删改权（变更前的集合认证、下一高度生效），重放随之逐高度跟随交接；节点间用 P2P gossip 传播交易（epidemic 泛洪去重）与认证块（反熵拉取追赶），每块对链上验证人集复验证书才应用——伪造/掉包证书停在缺口，确定性 `Network` 保证 N 节点收敛，同一纯状态机跑进程内与真实 TCP；验证人权重进一步**由质押背书**：账户自绑定 $COG 即成为验证人、权重等于绑定量，解绑经时间锁提款队列（资金留池、仍可罚没、到期返还），全程供应守恒；最后把问责闭环合上——链上 `slashing_evidence` 携带等价双签的密码学证据，验证后罚没作恶验证人的绑定质押与解绑中金额入 treasury、下一高度移出验证人集（供应守恒，坏证据整块回滚）；并把"块级 ops"扩展到 P2P：双签证据与质押变更经 gossip 内容哈希去重 flood 到每个节点的待打包池，出块方 `take_pending_*` 灌进 `ChainDriver`，下一区块自动把它们带出去——作恶可由任何节点远程检举并落地，不在依赖出块人已持有；最后给钱包/轻客户端一条捷径：`ValidatorTracker` 只凭创世信任根消费全节点已经 gossip 的 `(块, 证书)` 对，逐高度用当前集合复验最终性证书、再复刻该块引起的验证人集迁移，**不执行任何交易、不追踪账户**即得到与全量重放逐字节一致的活跃验证人集（输入全在 `block_hash` 内、由证书背书，可证明而非可信）；再把那份跟随收紧成 SPV 原语：把"下一高度生效的验证人集"的 Merkle 根 `next_validators_root` 折进区块头、纳入证书所签的 `block_hash`，轻客户端遂能**免复刻迁移**地凭一份证书验证整套下一集合，或用 O(log n) 包含证明对 cert 签名的头**证明单个验证人**——出块方共识前 `seal` 承诺根、`apply_block` 提交时强制匹配，迁移从不读该字段故无循环；最后把 SPV 搬到 gossip 总线上：`BlockHeader` 是 `Block` 的证书签名投影——除体外的全部字段、外加每份体的 SHA-256 承诺，`Block::hash` 现改为哈希同一份 header 投影，使 `header.hash() == block.hash()` 恒成立——`CertifiedHeader = (BlockHeader, Commit)` 比 `(Block, Commit)` 小一整个体字节，新 `GossipMsg::GetHeaders/Headers` + `LightGossipNode`（只保留头 + `ValidatorTracker`，从不反序列化交易）+ `LightNetwork` 把 SPV 原语搬上总线，`follow_header` 与 `verify_membership_against_header` 把跟随迁移至只对头形式；最后给钱包一步到位的"账户-成员 SPV"：把 `state_root`（完整共识状态 digest，由证书代验）与 `accounts_root`（accounts ∪ reviewers 二叉 Merkle 根，供 O(log n) 包含证明）两条承诺根也折进 `BlockHeader`，新 `GetAccountProof/AccountProof` 让钱包从对端要单账户包含证明，新 SPV `verify_account_membership_against_header` 在本地重算 leaf 并对头里的 `accounts_root` 验证——钱包证明自己的余额**只下头、不下体、零重放**；最后把那条 SPV 收紧成**批量化、类型化**的统一原语（M24）：M23 的 `GetAccountProof/AccountProof` 单账户单 trick 的传输被 M24 的 `GetProof { items }` / `Proof { items }` 一对**完全替换**（wire tags 8/9 复用给新对），承载任意混合 `[(Account|Reviewer|Validator, id), ...]` 列表，单次往返上限 `MAX_PROOF_BATCH = 32`；新 `Reviewer::merkle_leaf()` + `ChainState::reviewer_proof(id)` 闭合审阅人包含证明的路径（审阅人叶一直就在 `accounts_root` 二叉树里，只是没有 typed producer）；新 typed `ProofEntry` 枚举（Account/Reviewer/Validator 三变体）携带 typed leaf + proof，wallet 端 M22/M23 的 `verify_membership_against_header` / `verify_account_membership_against_header` **全部删除**，只剩**一个** `ValidatorTracker::verify_proof_against_header(header, cert, tracked_set, entry)` 调度器，按 `entry.kind()` 选根——Account/Reviewer 对 `accounts_root`，Validator 对 `next_validators_root`，本地重算 leaf、零信任 prover；`cmd_account` demo 把三证明（账户余额 + 审阅人声誉 + 验证人集合成员）放在一次 GetProof 里并发证，全部对同一 cert-signed 头成立。详见 [node/README.md](node/README.md)。

## 核心概念速查

| 术语 | 含义 |
|---|---|
| ΔK | 认知增量，PoK 中衡量新增认知的核心指标 |
| PoK / PoE / PoF | 认知证明 / 电力证明 / 有效算力证明 |
| $COG / $WATT / $FLOP | 认知币（主）/ 电力凭证 / 算力凭证 |
| cNFT | 认知贡献证书 |

## 状态

Draft v0.10 · RFC。所有参数、公式、经济模型均为待验证的初始设计，欢迎社区评审与 PR。

## License

见 [LICENSE](LICENSE)。
